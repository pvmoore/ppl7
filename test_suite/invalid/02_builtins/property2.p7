/**
 * magic!!
 * name "unsupported property type"
 * tags [builtin, property]
 * args []
 * errors ["@property() type must be one of"]
 */
public fn main() -> int {

    bool g = ::property(void, "sdfsds")

    return 0
}
