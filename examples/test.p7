import stdc = core:stdc
import vk   = vulkan:test_vulkan
import cstr = core:string
import core:console

public fn main() -> int {
    vk.testVulkan()

    # Green OK
    stdc.puts("\n\u001b[32;1mOK\u001b[0m"z)

    # fixme
    int p = 1
    int* pp = &p
    if(1) {
        stdc.puts(""z)
    }

    # int[3] array = [9,9,9]
    # int* ptr = &array

    #todo - this should probably be allowed
    # int[3] array2 = *ptr as int[3]

    #-----------------

    testPete()

    return 0
}

[[noinline]]
fn testPete() {

    #fixme
    #int[1.1] nope

    # DoubleEnum e
    # assert e is DoubleEnum.A
    # assert e as double is 1.0d

    # DoubleEnum a = DoubleEnum.A
    # DoubleEnum b = DoubleEnum.B

    # assert a as double is 1.1d
    # assert DoubleEnum.B as double is 2.1d

    # IntPtrStructEnum a = IntPtrStructEnum.A
    # IntPtrStructEnum b = IntPtrStructEnum.B
    # IntPtrStructEnum c = IntPtrStructEnum.C

    # assert b is IntPtrStructEnum.B

    assert IntPtrStructEnum.B is &g_one
    assert IntPtrStructEnum.C is not &g_one
}

enum DoubleEnum : double {
    A = 1.1d          # 1.1d
    B                 # 2.1d
}

int g_one
int g_two

enum IntPtrStructEnum : int* {
    A = null
    B = &g_one
    C = &g_two
}
