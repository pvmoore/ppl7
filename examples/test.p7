import stdc = core:stdc
import vk   = vulkan:test_vulkan
import cstr = core:string
import core:console

public fn main() -> int {
    vk.testVulkan()

    # Green OK
    stdc.puts("\n\u001b[32;1mOK\u001b[0m"z)

    # fixme
    int p = 1
    int* pp = &p
    if(1) {
        stdc.puts(""z)
    }

    int a = 0b1100_1100
    println(a)

    return 0
}

